<div class="comments">
  {{#each this}}
    <div class="post">
      <div class="row comment-header"></div>
      <div class="row post-body">
        <p>{{comment_text}}</p>
        <p>-{{user.username}}, {{format_date created_at}}</p>
      </div>
    </div>
  {{/each}}
</div>